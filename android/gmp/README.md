# Cross-Compilation of GMP for Android

## How to Compile and Install

Setup the standalone toolchain of AndroidNDK (if not, go to `../ndk-standalone`).
Then, execute:

```
$ . setenv.sh
$ make
$ make install
```
